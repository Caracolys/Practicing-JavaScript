function funcao() {

    var usuario = 'Carol'
    const aviso = "Esta é minha primeira interação com a ";
    
    usuario = document.getElementById("nome_curso").value;
    
    alert(aviso + usuario);
    
    }
